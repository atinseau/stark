/** Default service name for OpenTelemetry resource attributes. */
export const DEFAULT_SERVICE_NAME = "stark";

/** Default service version for OpenTelemetry resource attributes. */
export const DEFAULT_SERVICE_VERSION = "0.1.0";

/** Default tracer name used when calling `provider.getTracer()`. */
export const DEFAULT_TRACER_NAME = "stark";

/** Default tracer version used when calling `provider.getTracer()`. */
export const DEFAULT_TRACER_VERSION = "0.1.0";
